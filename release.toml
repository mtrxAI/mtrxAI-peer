# Canonical release version for client, desktop, and server.
# CI reads this file (or a v* git tag) and syncs all package manifests before building.
version = "0.1.5"

[docker]
registry = "docker.io/cosmicentropy"
client_image = "mtrxai-client"
server_image = "mtrxai-server"
