# Canonical release version for peer container and desktop builds.
# CI reads this file (or a v* git tag) and syncs package manifests before building.
version = "0.3.0"

[docker]
registry = "docker.io/mtrxai"
client_image = "mtrx-peer"
server_image = "mtrx-server"
